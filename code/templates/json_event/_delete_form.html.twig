<form method="post" action="{{ path('app_json_event_delete', {'id': json_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ json_event.id) }}">
    <button class="btn">Delete</button>
</form>
