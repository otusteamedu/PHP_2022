{% extends 'base.html.twig' %}

{% block title %}JsonEvent{% endblock %}

{% block body %}
    <h1>JsonEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ json_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ json_event.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ json_event.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_json_event_index') }}">back to list</a>

    <a href="{{ path('app_json_event_edit', {'id': json_event.id}) }}">edit</a>

    {{ include('json_event/_delete_form.html.twig') }}
{% endblock %}
