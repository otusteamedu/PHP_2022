<form method="post" action="{{ path('app_youtube_statistics_delete', {'id': youtube_statistic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ youtube_statistic.id) }}">
    <button class="btn">Delete</button>
</form>
