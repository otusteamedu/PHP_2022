version: "3.8"

services:
  postgres:
    image: postgres:latest
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pg-data2:/var/lib/postgresql/data/

volumes:
  pg-data2: