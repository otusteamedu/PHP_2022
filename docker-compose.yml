version: "3.9"

services:

  postgres:
    image: postgres:15.1
    environment:
      POSTGRES_DB: "test"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "pass"
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "5432:5432"