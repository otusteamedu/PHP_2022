version: '3.8'
services:

  php-cli:
    build:
      context: .docker
      dockerfile: ./php-cli/Dockerfile
    environment:
      PATH_TO_SOCKET: ${PATH_TO_SOCKET}
    volumes:
      - ./src:/app

  composer:
    image: composer
    volumes:
      - ./src:/app
    working_dir: /app