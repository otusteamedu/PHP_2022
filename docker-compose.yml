version: '3.8'
services:

  php-cli:
    build:
      context: .docker
      dockerfile: ./php-cli/Dockerfile
    volumes:
      - ./src:/app

  composer:
    image: composer
    volumes:
      - ./src:/app
    working_dir: /app